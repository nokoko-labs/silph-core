# ============================================
# Kime API - Environment Variables
# ============================================
# Copy this file to .env and fill in the values
# cp .env.example .env
#
# ⚠️  WARNING: All variables below are REQUIRED
# The application will NOT start if any are missing or invalid.
# ============================================

# Node Environment
# Options: dev, prod, test
NODE_ENV=dev

# Application Port
# Must be a positive integer
PORT=3000

# Database Connection URL
# PostgreSQL connection string
# Example for local Docker: postgresql://postgres:postgres@localhost:5432/kime_db?schema=public
# Example for Supabase: postgresql://user:password@host:5432/database?schema=public
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/kime_db?schema=public

# Redis Connection URL
# Redis connection string
# Example: redis://localhost:6379
# Example with password: redis://:password@localhost:6379
# Example with auth: redis://username:password@localhost:6379
REDIS_URL=redis://:redis@localhost:6379

# JWT Authentication
# Secret used to sign JWT tokens (required in production)
JWT_SECRET=your-jwt-secret-change-in-production
# Token expiration (e.g. 7d, 24h, 60m)
JWT_EXPIRES_IN=7d

# ============================================
# Seed (prisma db seed)
# ============================================
# Required in production when running seed. In dev, falls back to a default if unset.
# ADMIN_SEED_PASSWORD=

# ============================================
# Optional: Redis Individual Configuration
# (Only used if REDIS_URL is not provided)
# ============================================
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# ============================================
# Optional: Docker PostgreSQL Configuration
# (Only for docker-compose, not used by app)
# ============================================
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_DB=kime_db
# POSTGRES_PORT=5432
