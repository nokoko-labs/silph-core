name: Prepare Release PR
on:
  workflow_dispatch:

jobs:
  open-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Generate Token
        uses: actions/create-github-app-token@v1
        id: app-token
        with:
          app-id: ${{ secrets.BOT_APP_ID }}
          private-key: ${{ secrets.BOT_PRIVATE_KEY }}

      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ steps.app-token.outputs.token }}
      
      - name: Get current date
        id: date
        run: echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

      - name: Generate Changelog Preview
        id: changelog
        run: |
          git fetch origin main --depth=100 
          
          echo "CHANGELOG_CONTENT<<EOF" >> $GITHUB_ENV
          
          CHANGES=$(git log origin/main..HEAD --oneline --no-merges | sed 's/^/* /')
          
          if [ -z "$CHANGES" ]; then
            echo "* No new commits found (branches might be already in sync)." >> $GITHUB_ENV
          else
            echo "$CHANGES" >> $GITHUB_ENV
          fi
          
          echo "EOF" >> $GITHUB_ENV

      - name: Create Pull Request
        run: |
          gh pr create \
            --base main \
            --head develop \
            --title "chore(release): develop to main sync (${{ steps.date.outputs.date }})" \
            --body "## 🚀 Changes in this Release
          $CHANGELOG_CONTENT
          
          ---
          *This PR was automatically generated. Merging via **Squash** will trigger the versioning process.*"
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}